# PRD: ระบบติดตามวันครบกำหนด Review พนักงาน (v1)

## 1. บริบท
HR ต้องคอยไล่เช็คเองว่าพนักงานคนไหนใกล้หรือเกินวันครบกำหนด review (เช่น ช่วงทดลองงาน 90 วัน หรือ 119 วัน) ซึ่งตอนนี้ข้อมูลกระจัดกระจายและมีปริมาณมาก ทำให้ตกหล่นได้ง่าย แอปนี้เป็น **แบบ A (เครื่องมือส่วนตัว)** สำหรับ HR คนเดียว โดย core loop คือ: **HR บันทึกข้อมูลพนักงาน (ชื่อ, วันเริ่มงาน, วันครบกำหนด review) ได้ และ HR เห็นสถานะของแต่ละคน (ปกติ/ใกล้ครบ/เกินกำหนด) พร้อมจำนวนวันทำงานที่ผ่านมา ในหน้ารวมทันที**

## 2. ผู้ใช้และบทบาท
- มีผู้ใช้เดียวคือ **HR (เจ้าของแอป)** ต้อง login ก่อนถึงเข้าใช้งานได้ทั้งหมด (ทั้งแอปอยู่หลัง login)
- ไม่มีผู้ใช้อื่น — HR คนอื่นในแผนกยังเข้ามาดูพร้อมกันไม่ได้ใน v1 (ย้ายไป v2)

## 3. เส้นทางของผู้ใช้
1. เปิดเว็บ → เจอหน้า Landing (หน้าแนะนำระบบสั้น ๆ ดูได้โดยไม่ต้อง login)
2. กด "เข้าสู่ระบบ" → เจอหน้า login
3. Login ด้วยบัญชีเจ้าของ (Neon Auth)
4. เข้าสู่หน้ารวมพนักงาน เห็นสถานะทุกคนทันที
5. กด "เพิ่มพนักงาน" กรอกชื่อ / วันเริ่มงาน / วันครบกำหนด review → บันทึก
6. พนักงานคนใหม่ขึ้นในหน้ารวม พร้อมจำนวนวันที่ผ่านมาและสถานะ
7. กลับมาเปิดแอปวันไหนก็ตาม สถานะอัปเดตให้อัตโนมัติตามวันที่ปัจจุบัน
8. เข้าไปดูหน้าสรุปรายเดือน เพื่อดูภาพรวมว่าเดือนไหนมีกี่คนครบกำหนด

## 4. หน้าจอฝั่งผู้ใช้ (HR/เจ้าของ)
**หน้า Landing**
- หน้าแนะนำระบบสั้น ๆ เนื้อหาคงที่ ไม่ต้อง login ก็ดูได้ มีปุ่ม "เข้าสู่ระบบ" ไปหน้า login

**หน้า Login**
- กรอกอีเมล/รหัสผ่านของเจ้าของ → กด "เข้าสู่ระบบ" → ไปหน้ารวมพนักงาน

**หน้ารวมพนักงาน (หน้าหลัก)**
- ตารางพนักงานทั้งหมด: ชื่อ, วันเริ่มงาน, วันครบกำหนด review, จำนวนวันทำงานที่ผ่านมา, badge สถานะ (สีเขียว=ปกติ, สีเหลือง=ใกล้ครบ, สีแดง=เกินกำหนด)
- เรียงคนที่ใกล้/เกินกำหนดที่สุดไว้บนสุด
- ปุ่ม "+ เพิ่มพนักงาน" ที่มุมบน, ปุ่ม "แก้ไข"/"ลบ" ต่อแถว
- ปุ่ม/ลิงก์ไปหน้า "สรุปรายเดือน"

**หน้าเพิ่ม/แก้ไขพนักงาน (ฟอร์ม)**
- ช่องกรอก: ชื่อพนักงาน, วันเริ่มงาน (date picker), วันครบกำหนด review (date picker)
- ปุ่ม "บันทึก" → กลับไปหน้ารวมพร้อมข้อมูลใหม่ / ปุ่ม "ยกเลิก" → กลับไปหน้ารวมโดยไม่บันทึก

**หน้าสรุปรายเดือน**
- ตารางแสดงแต่ละเดือน + จำนวนคน/รายชื่อที่ครบกำหนด review ในเดือนนั้น
- เลือกดูเดือนย้อนหลังได้

## 5. หน้าจอฝั่งเจ้าของ
ไม่มี — ผู้ใช้คือเจ้าของ (แบบ A)

## 6. ข้อมูลที่ต้องเก็บ
เราเก็บ **ชื่อพนักงาน วันที่เริ่มงาน และวันที่ครบกำหนด review** ของพนักงานแต่ละคน ระบบจะคำนวณ "จำนวนวันที่ผ่านมา" และสถานะเองจากข้อมูลนี้ ไม่ต้องเก็บข้อมูลอื่นเพิ่ม

## 7. กฎและกรณีพิเศษ
- กรอกไม่ครบทั้ง 3 ช่อง (ชื่อ/วันเริ่มงาน/วันครบกำหนด) → บันทึกไม่ได้ ระบบเตือนให้กรอกให้ครบ
- วันครบกำหนด review ต้องอยู่หลังวันเริ่มงานเสมอ ถ้าใส่ผิดระบบเตือนก่อนบันทึก
- ลบพนักงาน → ระบบลบทันที ไม่มีถังขยะ/กู้คืนใน v1 (มีกล่องถามยืนยันก่อนลบเสมอ) — ยืนยันแล้ว
- นับ "จำนวนวันที่ผ่านมา" นับทุกวันตั้งแต่วันเริ่มงาน (ไม่เว้นเสาร์-อาทิตย์) — ยืนยันแล้ว
- สถานะ "ใกล้ครบ" = เหลือ ≤ 7 วันก่อนถึงวันครบกำหนด, "เกินกำหนด" = เลยวันครบกำหนดแล้ว, นอกนั้น = "ปกติ" — ยืนยันแล้ว
- หน้ารวมตอนยังไม่มีพนักงานเลย → แสดงข้อความ "ยังไม่มีพนักงานในระบบ กด + เพื่อเพิ่มคนแรก"
- มีผู้ใช้เดียว จึงไม่มีเคสสองคนแก้ข้อมูลพร้อมกัน

## 8. สิ่งที่ต้องเตรียมก่อนเริ่ม build
- [ ] ❌ รายชื่อพนักงานปัจจุบันทั้งหมด พร้อมวันเริ่มงานและวันครบกำหนด review ของแต่ละคน (ยังไม่ได้ส่งมา เตรียมเป็นลิสต์/Excel ไว้ก่อน build)
- [ ] ข้อความ/ชื่อแอปบนหน้าเว็บ — ถ้ายังไม่คิด จะใช้ค่า default ไปก่อน
- [ ] ไม่มีรูปที่ต้องใช้ในแอปนี้

## 9. v1 ถือว่าเสร็จเมื่อ...
1. Login เข้าระบบด้วยบัญชีเจ้าของได้
2. เพิ่มพนักงานใหม่ 1 คนแล้วเห็นในหน้ารวมทันที
3. หน้ารวมแสดงจำนวนวันที่ผ่านมาของแต่ละคนถูกต้อง
4. หน้ารวมแสดง badge สถานะ (ปกติ/ใกล้ครบ/เกินกำหนด) ตรงกับวันครบกำหนดที่ตั้งไว้จริง
5. แก้ไขข้อมูลพนักงานคนหนึ่งแล้วเห็นการเปลี่ยนแปลงอัปเดตในหน้ารวมทันที
6. ลบพนักงานคนหนึ่งแล้วหายไปจากหน้ารวม
7. เปิดหน้าสรุปรายเดือน เห็นจำนวนคน/รายชื่อที่ครบกำหนดในแต่ละเดือนถูกต้อง

## 10. ไม่ทำใน v1 (v2)
- แจ้งเตือนจริงผ่าน email/LINE/SMS เมื่อใกล้/เกินกำหนด — ฟีเจอร์แจ้งเตือนทุกแบบเป็น auto-v2 ของคลาส
- หลายบัญชี HR ในแผนกเข้าดูพร้อมกัน — ระบบหลายผู้ใช้เป็น auto-v2 ของคลาส
- คำนวณวันครบกำหนดอัตโนมัติแบบ fixed checkpoint 30/60/90/119 วันจากวันเริ่มงาน — ผู้ใช้เลือกให้ตั้งวันครบกำหนดเองต่อคนแทนใน v1 แล้ว
- กราฟแนวโน้มสรุปผลรายเดือนแบบซับซ้อน — ผู้ใช้เลือกใช้ตารางง่ายๆ พอสำหรับ v1
- Export/Import ข้อมูลเป็นไฟล์ — auto-v2 ของคลาส
- กู้คืนพนักงานที่ถูกลบ (soft-delete + หน้าถังขยะ) — ผู้ใช้อยากได้ แต่ตกลงว่าจะทำหลังเลิกคลาสเพื่อไม่ให้เกินเวลา
