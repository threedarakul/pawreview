# แผนงาน PawReview v1

อ้างอิงจาก `PRD.md`, `DESIGN.md`, และ mockup ใน `Design - ui/` ทำทีละ Phase แล้วหยุดรอ OK ก่อนไป Phase ถัดไป

## สรุปการตัดสินใจที่ยืนยันกับผู้ใช้แล้ว
1. **หน้า Landing** — ทำด้วย เพิ่มเข้าเส้นทางผู้ใช้เป็น: เปิดเว็บ → หน้า Landing → กด login → หน้ารวมพนักงาน
2. **ลบพนักงาน** — ลบทันทีแบบกู้คืนไม่ได้ (มีกล่องถามยืนยันก่อนลบ) ส่วนกู้คืนได้ (ถังขยะ) ยกไปทำหลังเลิกคลาส
3. **นับจำนวนวัน** — นับทุกวันตั้งแต่วันเริ่มงาน (ไม่เว้นเสาร์-อาทิตย์)
4. **เกณฑ์ "ใกล้ครบกำหนด"** — เหลือ ≤ 7 วันก่อนถึงวันครบกำหนด

## ของใน mockup ที่ไม่ทำใน v1 (ขัดกับ PRD ตรง ๆ)
- ปุ่มส่งออกข้อมูล Excel/CSV
- กราฟแท่งสรุปรายปีในหน้าสรุปรายเดือน
- ปุ่มบันทึกผลด่วน/นัดสัมภาษณ์ และสถิติ "เสร็จสิ้นแล้ว %" ในหน้าสรุปรายเดือน
- กล่องบัญชีทดลองบนหน้า login
- กระดิ่งแจ้งเตือน (ใส่เป็นไอคอนตกแต่งเฉย ๆ ไม่มีฟังก์ชัน)

ฟอร์มเพิ่ม/แก้ไขพนักงานจะทำเป็น popup ตาม mockup (แทนหน้าแยกที่ PRD อธิบายไว้ — แค่วิธีแสดงผล ฟังก์ชันเหมือนเดิม)

---

## Phase 0: เตรียมโปรเจกต์ + ขึ้นเว็บเปล่าให้ดูได้จริง
**Goal:** เตรียมเครื่องมือสร้างเว็บให้พร้อม ตั้งโทนสี/ฟอนต์ตามดีไซน์ แล้วเปิดเว็บเปล่าให้ดูได้จากมือถือจริงก่อนเริ่มทำหน้าตา

**งานที่ทำ:** สร้างโปรเจกต์ Next.js + Tailwind + shadcn/ui, ตั้งธีมสี/ฟอนต์จาก `DESIGN.md` (Warm Pastel Pulse) ใน `globals.css`, สร้าง Neon project (Singapore), เขียน `.env.local`, push ขึ้น GitHub, เชื่อม Vercel + Neon integration, deploy หน้าเปล่า

**วิธีทดสอบ:**
1. เปิดลิงก์เว็บจาก Vercel บนมือถือของคุณเอง
2. ต้องเห็นหน้าเว็บขึ้นสำเร็จ ไม่ใช่หน้า error

**Done when:** `npm run build` ผ่านในเครื่อง, เว็บ live เปิดได้จริงบน Vercel, มี `DATABASE_URL` ใน `.env.local` แล้ว, commit ไว้แล้ว

**Status:** [ ] ยังไม่เริ่ม

---

## Phase 1: หน้าตาทุกหน้า (ข้อมูลตัวอย่าง)
**Goal:** ทำหน้าตาเว็บทุกหน้าให้เหมือนภาพดีไซน์ ใส่ข้อมูลตัวอย่างไปก่อน ยังไม่บันทึกอะไรจริง

**หน้าที่สร้าง:** หน้า Landing (คงที่), หน้า Login (ยังไม่ผูก login จริง), หน้ารวมพนักงาน (ตารางตัวอย่าง 4-5 คน + badge สถานะ 3 สี + ปุ่ม "+เพิ่มพนักงาน" + ช่องค้นหา/แท็บกรองสถานะ + ปุ่มแก้ไข/ลบ), popup เพิ่ม/แก้ไขพนักงาน (ยังไม่เก็บจริง), หน้าสรุปรายเดือน (ตารางง่าย ๆ ตามเดือน)

**วิธีทดสอบ:**
1. เปิดหน้า Landing เห็นหน้าตาตรงกับดีไซน์
2. กดเข้าสู่ระบบ ไปหน้า login เห็นฟอร์มกรอกอีเมล/รหัสผ่าน
3. เปิดหน้ารวมพนักงาน เห็นตารางตัวอย่างพร้อม badge สี ลองค้นหา/กรองสถานะดู
4. กด "+เพิ่มพนักงาน" เห็นฟอร์ม popup ตามดีไซน์
5. กด "สรุปรายเดือน" เห็นตารางสรุปแยกตามเดือน
6. เปิดทุกหน้าบนมือถือ ดูว่าไม่ล้นจอ

**Done when:** `npm run lint` ผ่าน, `npm run build` ผ่าน, ทุกหน้าเปิดได้และตรงกับ mockup, commit ไว้แล้ว

**Status:** [ ] ยังไม่เริ่ม

---

## Phase 2: เพิ่มพนักงาน + หน้ารวมพนักงานใช้ข้อมูลจริง
**Goal:** กรอกข้อมูลพนักงานใหม่แล้วบันทึกเก็บไว้จริงได้ หน้ารวมพนักงานแสดงข้อมูลจริงพร้อมคำนวณจำนวนวันที่ผ่านมาและสถานะให้อัตโนมัติ

**งานที่ทำ:** อธิบายตารางข้อมูลพนักงาน (ชื่อ/วันเริ่มงาน/วันครบกำหนด) เป็นภาษาไทยให้ผู้ใช้ดูและรอ OK ก่อนสร้างตารางจริงด้วย `lib/db/schema.ts` + `drizzle-kit push`, ต่อฟอร์มให้บันทึกลงฐานข้อมูลจริงพร้อมตรวจสอบฝั่ง server, หน้ารวมดึงข้อมูลจริงมาแสดง (แทนข้อมูลตัวอย่าง), คำนวณจำนวนวัน (นับทุกวัน) และ badge สถานะ (ใกล้ครบ ≤7 วัน/เกินกำหนด/ปกติ), เรียงคนใกล้/เกินกำหนดไว้บนสุด, ข้อความตอนตารางว่าง

**วิธีทดสอบ:**
1. เปิดหน้ารวมพนักงาน (ยังไม่มีใครเลย) เห็นข้อความแจ้งว่ายังไม่มีพนักงาน
2. กด "+เพิ่มพนักงาน" กรอกครบแล้วบันทึก เห็นคนใหม่ขึ้นทันทีพร้อมจำนวนวัน/badge ถูกต้อง
3. ลองกรอกวันครบกำหนดก่อนวันเริ่มงาน ต้องเตือนก่อนบันทึกไม่ได้
4. ลองปล่อยบางช่องว่าง ต้องเตือนให้กรอกครบ

**Done when:** `npm run lint`/`build` ผ่าน, ทดสอบเพิ่มพนักงานจริง 2-3 คนแล้วเช็ค badge/จำนวนวันตรง, commit ไว้แล้ว

**Status:** [ ] ยังไม่เริ่ม

---

## Phase 3: แก้ไขข้อมูลพนักงาน
**Goal:** แก้ไขข้อมูลพนักงานที่มีอยู่แล้วได้ และเห็นการเปลี่ยนแปลงในหน้ารวมทันที

**งานที่ทำ:** ปุ่ม "แก้ไข" เปิด popup เดิมพร้อมข้อมูลเดิม, บันทึกแล้วอัปเดตข้อมูลจริง, ใช้กติกาตรวจสอบเดียวกับตอนเพิ่ม

**วิธีทดสอบ:**
1. กด "แก้ไข" ของพนักงานคนหนึ่ง เห็นข้อมูลเดิมอยู่ในฟอร์ม
2. เปลี่ยนวันครบกำหนดแล้วบันทึก
3. เห็นแถวนั้นอัปเดตทันที

**Done when:** `npm run lint`/`build` ผ่าน, ทดสอบแก้ไขจริงแล้วเห็นผลอัปเดตทันที, commit ไว้แล้ว

**Status:** [ ] ยังไม่เริ่ม

---

## Phase 4: ลบพนักงาน
**Goal:** ลบพนักงานที่ไม่ต้องการออกจากระบบได้ โดยระบบถามยืนยันก่อนลบทุกครั้ง

**งานที่ทำ:** ปุ่ม "ลบ" เปิดกล่องถามยืนยัน, กดยืนยันแล้วลบออกจากฐานข้อมูลจริง, แถวหายจากหน้ารวมและสรุปรายเดือนทันที

**วิธีทดสอบ:**
1. กด "ลบ" ของพนักงานทดสอบ 1 คน เห็นกล่องถามยืนยัน
2. กด "ยกเลิก" แถวต้องยังอยู่
3. กดลบใหม่แล้วยืนยันจริง แถวต้องหายไป

**Done when:** `npm run lint`/`build` ผ่าน, ทดสอบลบแล้วเช็คว่าลบเฉพาะคนที่ตั้งใจ, commit ไว้แล้ว

**Status:** [ ] ยังไม่เริ่ม

---

## Phase 5: หน้าสรุปรายเดือน (ข้อมูลจริง)
**Goal:** ดูภาพรวมว่าเดือนไหนมีพนักงานครบกำหนด review กี่คน พร้อมรายชื่อ จากข้อมูลจริง

**งานที่ทำ:** จัดกลุ่มพนักงานตามเดือน/ปีของวันครบกำหนด, แสดงจำนวนคนและรายชื่อต่อเดือน, เลือกดูเดือน/ปีอื่นย้อนหลังได้

**วิธีทดสอบ:**
1. เปิดหน้าสรุปรายเดือน เห็นเดือนปัจจุบันเป็นค่าเริ่มต้น
2. เทียบจำนวนคน/รายชื่อกับข้อมูลจริง ต้องตรงกัน
3. เปลี่ยนไปดูเดือนอื่น เห็นรายชื่อเปลี่ยนตาม

**Done when:** `npm run lint`/`build` ผ่าน, ทดสอบกับข้อมูลจริงอย่างน้อย 2 เดือนแล้วนับตรง, commit ไว้แล้ว

**Status:** [ ] ยังไม่เริ่ม

---

## Phase 6: ระบบ Login เจ้าของ (Neon Auth)
**Goal:** ล็อกให้ต้องเข้าสู่ระบบก่อนถึงจะเข้าเว็บ (ยกเว้นหน้า Landing) และมีแค่บัญชีเจ้าของคนเดียวเท่านั้นที่ใช้งานได้

**งานที่ทำ:** ต่อ Login ด้วย Neon Auth ตามขั้นตอนทางการ, กันทุกหน้ายกเว้น Landing ให้ต้อง login ก่อน, สร้างบัญชีเจ้าของ 1 บัญชี, ทดสอบอีเมลอื่นว่าเข้าไม่เห็นข้อมูล

**วิธีทดสอบ:**
1. ล็อกเอาท์แล้วเปิดลิงก์หน้ารวมพนักงานตรง ๆ ต้องถูกพาไปหน้า login
2. ล็อกอินด้วยบัญชีเจ้าของ เข้าใช้งานได้ปกติ
3. ลองอีเมลอื่น ต้องเข้าไม่เห็นข้อมูลของเจ้าของ

**Done when:** `npm run lint`/`build` ผ่าน, ทดสอบแยกบัญชีสำเร็จ, เพิ่มค่าที่ Vercel ต้องการสำหรับ login แล้ว, commit + push แล้ว

**หมายเหตุ:** ถ้าตั้งค่า login ไม่ผ่าน 2 รอบ จะหยุดและแนะนำให้ติดต่อผู้สอน

**Status:** [ ] ยังไม่เริ่ม

---

## Phase 7: ตรวจครบและยืนยันเว็บจริงรอบสุดท้าย
**Goal:** เช็คของจริงบนเว็บที่ใช้งานได้แล้วให้ครบทุกอย่างตามที่ตั้งใจไว้ตั้งแต่ต้น

**งานที่ทำ:** push โค้ดสุดท้ายขึ้น GitHub, ไล่เช็คทีละข้อตาม checklist "v1 ถือว่าเสร็จเมื่อ" ของ PRD บนเว็บจริง

**วิธีทดสอบ:**
1. Login เข้าระบบด้วยบัญชีเจ้าของได้
2. เพิ่มพนักงานใหม่ 1 คนแล้วเห็นในหน้ารวมทันที
3. หน้ารวมแสดงจำนวนวันที่ผ่านมาถูกต้อง
4. หน้ารวมแสดง badge สถานะตรงกับวันครบกำหนดจริง
5. แก้ไขข้อมูลแล้วเห็นอัปเดตทันที
6. ลบพนักงานแล้วหายไปจากหน้ารวม
7. เปิดหน้าสรุปรายเดือน เห็นจำนวนคน/รายชื่อถูกต้อง

**Done when:** ครบทุกข้อบนเว็บจริง ไม่มี error ให้เห็น ผู้ใช้เปิดจากมือถือแล้วยืนยันว่าใช้ได้

**Status:** [ ] ยังไม่เริ่ม

---

## ตาราง map checklist PRD → Phase

| ข้อ checklist (PRD ข้อ 9) | Phase |
|---|---|
| 1. Login ด้วยบัญชีเจ้าของ | Phase 6 |
| 2. เพิ่มพนักงานแล้วเห็นในหน้ารวมทันที | Phase 2 |
| 3. แสดงจำนวนวันที่ผ่านมาถูกต้อง | Phase 2 |
| 4. แสดง badge สถานะถูกต้อง | Phase 2 |
| 5. แก้ไขแล้วอัปเดตทันที | Phase 3 |
| 6. ลบแล้วหายจากหน้ารวม | Phase 4 |
| 7. หน้าสรุปรายเดือนถูกต้อง | Phase 5 |
